# Web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
gunicorn>=21.2,<22

# DB + async driver
sqlalchemy>=2.0,<3
asyncpg>=0.29,<1

# Pydantic v2 stack + env
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-dotenv>=1.0,<2

# HTTP clients
requests>=2.32,<3
httpx>=0.27,<1
python-multipart>=0.0.9,<1   # needed if you ever parse forms/uploads

# OpenAI + LangChain (use v1 OpenAI; required by langchain-openai)
openai>=1.40,<2
langchain-core>=0.2.41,<1
langchain-openai>=0.1.22,<1

# AWS SDK
boto3>=1.34,<2

# Optional perf (FastAPI can use this automatically if present)
orjson>=3.10,<4
