# --- Core API
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
gunicorn>=21.2,<22.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-multipart>=0.0.9    # form/file uploads

# --- Database (SQLAlchemy async; Postgres + optional SQLite for local dev)
sqlalchemy>=2.0,<3.0
asyncpg>=0.29              # Postgres driver
aiosqlite>=0.20            # local/dev only (sqlite)

# --- AWS (Secrets Manager, etc.)
boto3>=1.34

# --- OpenAI + LangChain
openai>=1.30.0
tiktoken>=0.7.0
langchain-core>=0.2
langchain-community>=0.2
langchain-openai>=0.1

# --- Caching / Queues
redis>=5.0

# --- HTTP clients / utilities
requests>=2.32
httpx>=0.27
tenacity>=8.2

# --- Optional: migrations (uncomment if you use Alembic)
# alembic>=1.13
rapidfuzz>=3.9.0
Pillow>=10.4.0
langgraph>=0.2
